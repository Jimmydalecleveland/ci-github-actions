name: CD & CI Practice
on: [push]
jobs:
  eslint:
    name: Code Linting
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install Packages
        run: npm i
      - name: Run Eslint
        run: npm run lint
